[tool.ruff]
line-length = 100
exclude = [
    ".venv",
    "__pycache__",
    "reports",
    ".git"
]

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I"
]

[tool.ruff.lint.per-file-ignores]
"conftest.py" = ["E402"]